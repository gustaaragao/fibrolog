## ADDED Requirements

### Requirement: Notify Support Network
The system SHALL provide a prominent button to allow the user to quickly contact their support network during a crisis.

#### Scenario: Call support contact
- **WHEN** user taps the "Notify Support Network" button and selects a contact
- **THEN** the system SHALL initiate a phone call to the selected contact's phone number using the device's native dialer
