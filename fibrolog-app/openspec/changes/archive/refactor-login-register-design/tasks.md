## 1. UI Component Updates

- [x] 1.1 Extend `components/ui/Input.tsx` to support `Controller` and validation styling if needed
- [x] 1.2 Ensure `components/ui/Button.tsx` has the correct variants for the refactored design

## 2. Login Screen Refactoring

- [x] 2.1 Refactor `app/login.tsx` to use NativeWind for layout and styling
- [x] 2.2 Replace custom inputs in `app/login.tsx` with `components/ui/Input.tsx`
- [x] 2.3 Replace custom button in `app/login.tsx` with `components/ui/Button.tsx`
- [x] 2.4 Apply `Carattere_400Regular` font to the \"FibroLog\" title in `app/login.tsx`

## 3. Register Screen Refactoring

- [x] 3.1 Refactor `app/register.tsx` to use NativeWind for layout and styling
- [x] 3.2 Replace custom inputs in `app/register.tsx` with `components/ui/Input.tsx`
- [x] 3.3 Replace custom button/radio buttons in `app/register.tsx` with shared or updated components
- [x] 3.4 Apply `Carattere_400Regular` font to the \"FibroLog\" title in `app/register.tsx`

## 4. Verification

- [x] 4.1 Run type checking to ensure no regressions
- [x] 4.2 Verify visual alignment with `HOME.png` design principles
- [x] 4.3 Test authentication flows (Login and Register) with new UI
