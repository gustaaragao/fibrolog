## 1. UI Implementation

- [x] 1.1 Update `app/symptoms.tsx` with the symptom selection grid (Sintoma 1).
- [x] 1.2 Implement the intensity and notes detail section (Sintoma 2).
- [x] 1.3 Apply pink theme styling to all components using `nativewind` and `ThemeColors`.

## 2. Logic and State

- [x] 2.1 Implement multi-step navigation/toggle between selection and details.
- [x] 2.2 Add symptom selection and intensity tracking state.
- [x] 2.3 Integrate `react-hook-form` for data management if necessary.

## 3. API and Integration

- [x] 3.1 Create a placeholder service in `services/api.ts` for symptom submission.
- [x] 3.2 Implement the submission logic with success/error feedback.
- [x] 3.3 Create the API refactoring prompt for the backend as a separate document or comment.

## 4. Verification

- [x] 4.1 Verify that selected symptoms and intensities are correctly captured.
- [x] 4.2 Test the navigation flow between the two states.
- [x] 4.3 Ensure the UI matches the provided designs and theme.