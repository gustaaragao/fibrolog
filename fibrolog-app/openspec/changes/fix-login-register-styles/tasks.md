## 1. Component Refinement

- [x] 1.1 Update `components/ui/Input.tsx` to use `pink-500` for focus state and refine border styling
- [x] 1.2 Update `components/ui/Button.tsx` to standardize `primary` variant to `pink-500` and refine `text` variant color

## 2. Screen Refinement

- [x] 2.1 Refine `app/login.tsx` layout, vertical spacing, and logo positioning
- [x] 2.2 Refine `app/register.tsx` layout, spacing, and consistency with login screen

## 3. Verification

- [x] 3.1 Verify visual alignment of all entry screens with the brand identity
- [x] 3.2 Run type checking and ensure application builds correctly
